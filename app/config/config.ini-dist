[config]

;Path to the folder used to store Git/SVN repositories
storage_path=/path/to/your/folder

;Leave empty if the website will be served from the root,
;"name_subfolder" if it will be served from a subfolder (e.g. "webstatus")
web_folder=
